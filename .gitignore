# Dependencies
node_modules/
.pnp.*

# Build output
dist/
apps/web/dist/
packages/*/dist/
dist-bridge/

# Tauri / Rust build
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# Bundled binaries — large; built/fetched locally (see apps/desktop/README.md).
# Kept out of git to keep the repo lean.
apps/desktop/src-tauri/binaries/
apps/desktop/src-tauri/sidecars/

# Local project data + caches
**/.transcripts/
*.log
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
!.env.example
*.pem
**/auth.json

# moon runtime cache (legacy)
.moon/cache/

# Internal working docs & session scratch (kept local, out of the public repo)
AUDIT.md
FASE28-PIANO.md
STRATEGIA-2026.md
*-this-session-is-being-continued-*.txt
.tokeignore

# Leftover temp files from an interrupted `bun build --compile`
*.bun-build
